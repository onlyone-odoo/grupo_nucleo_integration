<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Sync sale taxes from API: set each GN product's taxes_id from catalog (21% or 10.5% per API). -->
        <record id="action_gruponucleo_sync_sale_taxes_from_api" model="ir.actions.server">
            <field name="name">Grupo Núcleo: Sincronizar impuestos venta desde API</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="binding_model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">result = model.browse()._gruponucleo_sync_sale_taxes_from_api()</field>
        </record>
    </data>
</odoo>
